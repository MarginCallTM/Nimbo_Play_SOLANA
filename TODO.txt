==============================================================
==============================================================
  TODO — NIMBO PLAY  (écosystème de jeux P2E de SKILL sur Solana)
  PIVOT 2026-07-10 (cf CLAUDE.md §0 + DECISIONS.md §10, D42–D70)
    -> PRODUIT PHARE : jeux d'arcade PvP à mise en SOL (1er jeu =
       clone slither.io). Human vs human, extraction par le skill.
    -> LOTERIE : MVP quasi complet, reléguée en SIDE-FEATURE
       (gacha à skins). Détail en PARTIE 2 de ce fichier.
  RÈGLE D'OR : UN jeu FUN d'abord (prototype jetable, ZÉRO Solana)
  avant d'investir contrat / netcode de prod / monétisation.
  Le socle on-chain = AGNOSTIQUE du jeu (escrow générique).
  Testnet avant mainnet.
  Ordre : Fun -> Netcode autoritatif -> Escrow on-chain ->
          Intégration -> Anti-triche/matchmaking -> Monétisation ->
          Bloquants mainnet.
  NB : l'historique détaillé des tâches loterie terminées (phases
  A, 0..9, 10.A-C) a été condensé le 2026-07-23 ; le détail vit
  dans l'historique git de TODO.txt (<= commit 7b00569).
==============================================================
==============================================================


##############################################################
  PARTIE 1 — PRODUIT PHARE : ARENA (jeu P2E)
  >>> PROCHAINE ÉTAPE : PHASE ARENA-3 (A3.1 — SIWS) <<<
  DÉCISIONS DE REPRISE (user, 2026-07-30) : dev ARENA-3 directement
  sur DEVNET (le "testnet" des docs = devnet en pratique Solana) ;
  wallet branché dans arena/client (Vite vanilla, provider injecté),
  le portail Next.js attendra ARENA-5 ; service settlement A3.3 en
  Rust RETAPÉ À LA MAIN (même discipline que l'on-chain).
##############################################################

  Décisions encore OUVERTES (à trancher en cours de route) :
   - Cadence / placement des extract points (à régler au playtest ARENA-0).
   - Formule serveur de "la valeur d'un joueur" — DEMI-TRANCHÉE (2026-07-23) :
     le règlement se fait TOUJOURS sur le SCORE (registre comptable :
     SOL -> score au join à taux fixe, conservation stricte en jeu,
     score -> SOL à l'extraction au même taux). La TAILLE = f(score) est
     un AFFICHAGE (describeSnakeFromScore), jamais une entrée du calcul
     d'argent -> les courbes de croissance sont des curseurs d'équilibrage
     librement réglables au playtest. Reste à fixer : le taux SOL<->score.
   - Modèle A vs B pour le settlement : A (serveur = authority signataire)
     RECOMMANDÉ pour le MVP, B (attestation ed25519) = durcissement mainnet.
     Les tâches A2.x/A3.x ci-dessous supposent le modèle A.

  RÉFÉRENCE D'ÉTUDE (choisie le 2026-07-23) : littensy/slither
   https://github.com/littensy/slither  (MIT, maintenu, TS)
   /!\ Jeu ROBLOX (roblox-ts -> Luau) : PAS une base de code (rien ne
   tourne en navigateur) -> on ÉTUDIE l'approche, on ÉCRIT notre code.
   Intérêt : architecture = exactement notre cible (simulation autoritative
   serveur, client = rendu seul, types partagés, tests + bench du tick).
   Correspondances (à lire AVANT d'implémenter chaque étape) :
    - A0.2/A0.3 mouvement & segments -> src/server/world/services/snakes/snake-tick.ts
    - A0.4 collisions + grille       -> snakes/snake-grid.ts + collision/collision-tick.ts
    - A0.5 bots                      -> src/server/bots/bot-behavior.ts
    - ARENA-1 tick serveur & perfs   -> world.server.ts + test/world/world-tick.bench.ts
   REJETÉ : knagaitsev/slither.io-clone (AUCUNE licence = inexploitable
   légalement ; Phaser 2.6 abandonné 2018 ; client-only). Ne pas y revenir.


--------------------------------------------------------------
PHASE ARENA-0 — PROTOTYPE DE FUN  (jetable, ZÉRO Solana, ZÉRO réseau)
  But : répondre à UNE question -> est-ce que le jeu est KIFFANT ?
  Rien d'autre ne commence tant que ce n'est pas un OUI franc.
--------------------------------------------------------------
[x] A0.1  Setup client : PixiJS + TypeScript, boucle requestAnimationFrame
          HORS React (Pixi monté dans un ref, pas de re-render 60fps).
          -> FAIT (2026-07-23) : proto/ scaffoldé (Vite vanilla-ts, npm), pixi.js v8,
             boucle ticker + deltaTime validée (balle fluide plein écran).
[x] A0.2  Boucle single-player : 1 serpent contrôlable (souris), caméra qui
          suit, map bornée, rendu fluide de milliers d'entités (sprites batchés).
          + BOOST (clic maintenu = vitesse x2 ; réf littensy SNAKE_BOOST_SPEED).
          -> FAIT (2026-07-23) : serpent souris (angle/desiredAngle, braquage
             borné), follow-the-leader par lerp, caméra container, bordure
             glissante, boost clic/espace. Sprites batchés -> validés en A0.3.
          -> NB : validé au feel utilisateur ; le "rendu de milliers
             d'entités" sera réellement prouvé avec les pellets (A0.3).
[x] A0.3  Bouffe ambiante (gameplay only, SANS valeur) : le serpent grossit
          en mangeant ; introduit la variable SCORE dont tout dérive
          (longueur/rayon/turn speed, réf describeSnakeFromScore).
          + COÛT GAMEPLAY DU BOOST (même step, c'est la boucle complète) :
            sprint = score qui baisse + traînée d'orbes mangeables semées
            à la queue (réf dropCandyWhileBoosting) + PLANCHER : à la
            taille de base le sprint se coupe (réf snakeIsBoosting).
          La SÉMANTIQUE DE VALEUR de ces orbes (mock SOL, conservation
          exacte) reste en A0.6/A0.7.
          -> FAIT (2026-07-23, validé playtest) : grille spatiale (grid.ts),
             1000 pellets en sprites batchés 1 texture (food.ts), score ->
             describeSnakeFromScore (longueur/rayon/braquage), drain boost
             par quanta d'orbes 1.5 (conservation exacte, plancher OK),
             aimant 25px, HUD score/length/fps. Le corps du serpent reste
             un Graphics par frame : à passer en sprites si le FPS plie.
[x] A0.4  Collisions : serpent vs bouffe (grossir), tête vs corps adverse (mort).
          Partitionnement spatial (grille) DÈS le proto (O(n^2) = mort).
          -> FAIT (2026-07-23, validé playtest) : collision.ts (grille des
             segments REBUILD par frame, broad/narrow phase), règles slither
             (tête vs corps adverse = mort, bordure mortelle, se croiser
             soi-même légal, tête-à-tête = double mort), cadavre = score
             entier semé en orbes couleur du mort (conservation exacte),
             main.ts restructuré multi-serpents (entités {snake,gfx}).
[x] A0.5  Bots basiques (IA simple) pour peupler l'arène et tester le ressenti PvP.
          -> FAIT (2026-07-23, validé playtest) : bot.ts, priority steering
             (1 survivre > 2 manger > 3 errer), perception via les MÊMES
             grilles spatiales (probe devant la tête, vision bouffe 400px),
             6 bots (palettes distinctes), score de spawn aléatoire 20-80
             (embryon du buy-in variable), respawn auto. Tuning post-
             playtest : courbes score->taille ralenties et sorties en
             constantes (GROWTH_LENGTH_PER_SCORE 0.3, GROWTH_RADIUS_FACTOR
             0.02) — principe acté : la VALEUR (score) est sacrée, la
             TAILLE n'est que son affichage.
[x] A0.6  Mécanique de VALEUR mockée (pas de SOL) : taille = "valeur" affichée ;
          extract points qui spawn périodiquement ; timer de canalisation 4s vulnérable.
          -> FAIT (2026-07-23, validé playtest) : extract.ts (machine à états
             cooldown/actif/fermé ; événements one-frame spawned/extracted/
             detonated vs états continus), canalisation 4s PUREMENT SPATIALE
             (sortir = hard reset), cash-out = la valeur QUITTE l'arène (pas
             de cadavre). Itérations post-playtest : map x2.25 (rayon 3000),
             minimap slither (joueur + extract SEULEMENT, fog of war), banner
             d'alerte au spawn, chrono à l'écran, fermeture façon BOMBE
             (3 clignotements rouges + gonflement) qui TUE quiconque est
             dans la zone (mort normale -> loot sur place). Étiquettes de
             valeur mock-◎ au-dessus de chaque serpent.
[x] A0.7  Règle de mort — RÉVISÉE (user, 2026-07-23) : 70% de la valeur reste
          sur le cadavre (loot du tueur), 30% RECYCLÉ MAP-WIDE en pellets
          classiques à prix unitaire fixe (offre ambiante auto-gérée par
          l'activité mortelle + frein anti-snowball). Reliquat sub-pellet
          -> cadavre (conservation exacte). Conservation visible à l'écran.
          (Version initiale 90/10 avec scatter local : remplacée.)
          + SÉMANTIQUE DE VALEUR (dette d'A0.3/A0.6, tranchée ici) : séparer
          SCORE (croissance gameplay, gagné sur TOUTE bouffe) et VALUE
          (mock-SOL, conservée). DÉCISION USER 2026-07-23 (amende le
          "bouffe ambiante sans valeur" du CLAUDE.md §0) : la bouffe
          ambiante nourrit AUSSI la value, mais FINANCÉE par une réserve
          finie (mock de la FoodReserve PDA d'A2.2/A2.6 : SOL orphelin
          balayé entre rounds + injections maison) — jamais mintée du
          néant, sinon sum(extractions) > sum(dépôts) = INSOLVABLE.
          RAFFINÉ (user, 2026-07-23) : AUCUN pellet sans valeur — un
          pellet ne spawn que si la réserve le paie ; réserve vide =
          l'OFFRE se raréfie (respawn sauté, map qui se vide). L'afflux
          vient du trafic : churn des bots (durée de vie 30-90s -> crash,
          cadavre 70/30, nouvelle mise au respawn). Invariant HUD :
          arena + banked + reserve = constante + buy-ins.
[x] A0.7bis MENU DE LANCEMENT (idée user 2026-07-23, validée — confort de
          test + socle produit) : écran simple par-dessus le canvas (overlay
          DOM, pas de UI Pixi) — pseudo optionnel (affiché au-dessus du
          serpent), bouton JOUER. À la mort : retour menu + "rejouer"
          (préfigure le "repayer pour respawn"). FUSIONNE avec A0.8 : c'est
          sur cet écran que vit la mise (boutons présélectionnés).
          TIERS (user, 2026-07-23) : FREE / 0.10 / 0.25 / 0.50 / 1 SOL.
          FREE = taille fictive, valeur 0 ; dans le proto le worth mangé
          RETOURNE à la réserve (pas de passager clandestin). /!\ EN PROD :
          le mode free vit sur un SERVEUR SÉPARÉ (arène sans argent) —
          jamais mélangé aux arènes payantes (free-rider = faucet à bots).
[x] A0.8  Buy-in variable mocké : spawn plus gros si "mise" plus grande.
          -> FAIT (2026-07-23) : mise brute -> taille (score = stake/taux),
             valeur nette après split D71. Tiers : FREE / 0.10 / 0.25 /
             0.50 / 1 SOL. Bots recalibrés sur les mêmes tiers (0.10-0.60).
[x] A0.x  DURCISSEMENTS POST-PLAYTEST (2026-07-23, validés) :
          - Grâce de spawn 3s (translucide, intangible, immortel, ne
            ramasse rien) -> fix des spawnkills du churn.
          - Clamp du deltaTime (MAX_DT=3) -> fix du tunneling des têtes
            à travers les corps lors des micro-freezes. Leçon ARENA-1 :
            le serveur aura un tick FIXE pour la même raison.
          - Rendu des serpents en SPRITES batchés (1 texture partagée)
            -> fix des frames blanches du gros Graphics + perfs.
          - Fix "corps qui disparaît au sprint" (gros serpents) : le
            follow-the-leader lerpait vers la position DÉJÀ MISE À JOUR
            du prédécesseur -> à alpha=1 (boost + frame lente) tout le
            corps s'effondrait sur la tête en 1 tick (ni rendu ni
            hitbox). Fix : chaque tracer suit la position PRÉ-update.
            Prouvé par simulation (12000px -> 0px avant, stable après).
            Leçon ARENA-1 : bug de pas de temps VARIABLE -> tick fixe
            serveur (A1.3) élimine cette classe de bugs.
[x] ==> JALON GO/NO-GO : PLAYTEST -> GO (user, 2026-07-23). Le proto est
    jugé assez fun pour investir le netcode ; le tuning gameplay reste
    ouvert en continu (constantes = curseurs), les MÉCANIQUES sont figées.


--------------------------------------------------------------
PHASE ARENA-1 — SERVEUR AUTORITATIF & NETCODE  (toujours ZÉRO Solana)
  But : multijoueur temps réel, serveur-autoritatif (= fondation anti-triche).
  Le client n'envoie QUE ses inputs ; le serveur simule TOUT.
--------------------------------------------------------------
[x] A1.1  Choix serveur MVP : Node + Colyseus (rooms autoritatives, schéma
          binaire, matchmaking de base). Types TS partagés client/serveur.
          -> FAIT (2026-07-23, validé 2 onglets) : monorepo npm workspaces
             arena/ (shared/ = contrat protocole, server/ = Colyseus 0.17,
             client/ = Vite + @colyseus/sdk). API 0.17 VÉRIFIÉE en doc
             (defineServer/defineRoom, messages en objet, Callbacks.get) —
             différente de mon cut-off (0.16). PROTOCOL_VERSION rejette
             les clients périmés au join. Room "arena" : state MapSchema
             joueurs, join/leave/input prouvés, deltas binaires observés
             (2 onglets synchro sans refresh). proto/ intouché (référence).
[x] A1.2  Client = inputs seulement (direction, boost). ZÉRO calcul de
          position / kill / taille côté client (base de l'anti-triche).
          -> FAIT (2026-07-24, validé 2 onglets + tests de triche) : le
             serveur simule le mouvement (setSimulationInterval, ms ->
             frames 60fps), Player = champs @type synchronisés (float32
             = moitié de la bande passante de float64) + intent NON
             décoré (desiredAngle/wantsBoost = jamais envoyé). Input
             validé côté serveur (Number.isFinite — NaN/Infinity/string
             rejetés), messages hors liste blanche ignorés. Client =
             souris -> angle envoyé à 20Hz fixe (jamais par mousemove),
             rendu canvas 2D monde entier (Pixi porté plus tard).
          -> PIÈGE RÉSOLU (crash serveur au 1er join, "Failed to
             fetch" au refresh) : décorateurs legacy @colyseus/schema
             = setters de prototype, contournés par les champs de
             classe ES2022 -> MapSchema sans $childType -> crash à
             l'encodage. FIX : useDefineForClassFields:false dans le
             tsconfig serveur (obligatoire avec experimentalDecorators
             + target ES2022). Reproduit et prouvé par script
             join/leave/rejoin avant/après.
          -> Leçon outillage : tsx watch ne relance PAS après un crash
             (attend un changement de fichier) ; "Failed to fetch" =
             toujours vérifier lsof -iTCP:2567 -sTCP:LISTEN.
[x] A1.3  Tick de simulation fixe (~30Hz) DÉCOUPLÉ du broadcast (~15-20Hz).
          -> FAIT (2026-07-24, validé multi-onglets) : accumulateur à
             pas fixe (STEP 33.3ms, tick dt=TICK_DT constant — jamais
             l'horloge), garde anti-spirale (250ms max/frame, on JETTE
             le temps), setPatchRate(50ms). SERVER_TICK_RATE=30 /
             BROADCAST_RATE=20 dans shared (la prediction A1.5 doit
             simuler au même pas). Déterminisme par construction : la
             classe de bugs "dépend du framerate" (corps disparu du
             proto) ne peut plus exister. Collisions/gameplay pas
             encore portés — attendu à ce stade.
[x] A1.4  Sérialisation BINAIRE des snapshots (pas de JSON).
          -> FAIT (2026-07-24) : déjà fournie par @colyseus/schema
             (deltas binaires, champs indexés, float32) — travail =
             la MESURER sur le fil (wrap de connection.events.onmessage,
             compteur B/s dans le client, instrumentation jetable).
             BASELINE MESURÉE (2 joueurs en mouvement) : 750 B/s,
             20 msg/s, ~38 B/patch vs ~6000 B/s en JSON plein état
             naïf -> x8. Référence pour LA décision à venir : quand
             les serpents auront un corps, on ne synchronisera PAS
             les tracers (le client les reconstruira depuis la tête,
             follow-the-leader local) — à re-mesurer à ce moment-là.
[x] A1.5  Netcode : client-side prediction (ton serpent bouge immédiatement).
          -> FAIT (2026-07-24, validé à 200ms de lag simulé : contrôle
             "agréable", tapé à la main par l'utilisateur — cœur d'algo) :
             predictTick côté client = COPIE de ArenaRoom.tick (mêmes
             fonctions/constantes via shared/ — raison d'être du paquet),
             piloté par l'input LOCAL ; dt rAF borné à 3 frames (MAX_DT) ;
             correction dure : snap si écart > 150px (A1.7 = adoucir) ;
             visée souris depuis la position prédite (boucle input->
             affichage 100% locale). Debug : fantôme = position self vue
             serveur (l'écart visualise le RTT). Serveur : simulateLatency
             optionnel via SIMULATE_LATENCY_MS. Léger écart fantôme même
             sans lag = normal (patchs 20Hz + phase de tick, ~50-80ms).
[x] A1.6  Netcode : entity interpolation (les autres serpents rendus ~100ms
          dans le passé, lissés entre snapshots).
          -> FAIT (2026-07-24, cœur tapé par l'utilisateur ; validé à
             200ms de lag + analyse vidéo externe : aucune saccade du
             joueur distant) : buffer d'instantanés horodatés par joueur
             (room.onStateChange, garde 1s), rendu à now-100ms entre les
             2 photos encadrantes (lerp x/y + lerpAngle wrap [-PI,PI]),
             famine réseau -> HOLD dernière photo, JAMAIS d'extrapolation.
             Devise : soi dans le futur, les autres dans le passé.
          -> Bug pédagogique attrapé à la relecture de frappe : signe
             inversé dans la boucle de wrap (diff > -PI au lieu de <)
             = boucle infinie -> onglet gelé. Piège classique du wrap.
          -> L'analyse vidéo confirme en revanche le RUBBER-BANDING du
             self en virage serré + sprint (écart > 150px -> snap) :
             c'est la faiblesse assumée du snap dur d'A1.5 -> A1.7.
[x] A1.7  Netcode : server reconciliation (correction douce des écarts de prediction).
          -> FAIT (2026-07-24, validé : le claquement en virage sprinté
             a disparu à 200ms de lag) : le snap dur d'A1.5 péchait 2x
             (comparait le PASSÉ serveur au PRÉSENT client -> faux
             positifs ~vitesse*âge ; et corrigeait 100% en 1 frame).
             Fix : ping/pong applicatif -> RTT lissé EMA 80/20 ;
             historique horodaté des prédictions (1s) ; erreur VRAIE =
             vérité serveur vs notre croyance au MÊME instant (âge =
             RTT/2 + intervalle de patch) ; absorption 5%/frame
             (CORRECTION_RATE, curseur) ; snap réservé aux vraies
             téléportations (>400px). Angle jamais lissé (l'input le
             gouverne). => TRIPTYQUE NETCODE COMPLET (A1.5/6/7).
[ ] A1.7bis PORT DE LA SIMULATION proto -> serveur (ajouté 2026-07-24 :
          le TODO listait le netcode mais jamais explicitement le port
          du gameplay — sous-entendu par A1.10, désormais tâche à part).
          Ordre choisi : AVANT A1.8, car l'AoI n'a de sens qu'avec des
          milliers d'entités (et se mesurera au compteur réseau A1.4).
          RÈGLE DE SYNC (décidée en A1.4) : on ne synchronise JAMAIS
          les corps — tête + score suffisent, le client reconstruit
          (follow-the-leader local, comme le proto).
    [x] a. shared/ : maths du serpent (describeSnakeFromScore, constantes
          de croissance/espacement) — le client en aura besoin pour
          reconstruire les corps. FAIT 2026-07-24.
    [x] b. serveur : corps simulés au tick fixe (tracers follow-the-leader
          version pré-update du proto), sync = tête+score+boost only.
          Client : reconstruction locale du corps + rendu cercles canvas.
          FAIT 2026-07-24, validé : corps fluides, réseau inchangé
          (~750 B/s pour 2 serpents à corps = preuve mesurée de la
          règle de sync). + FIX zone morte de visée (60px monde) :
          atan2 instable curseur-près-de-la-tête cassait les cercles
          serrés (comparé au vrai slither.io par l'utilisateur : feel
          identique). Clamp bordure prediction aligné sur le rayon
          dérivé du score (miroir exact du serveur).
    [x] c. bouffe : FoodField serveur (grille spatiale du proto) +
          MapSchema (add/remove only) ; manger -> score -> croissance.
          FAIT 2026-07-24, validé : 2200 pellets (sqrt(random) =
          densité uniforme), grille STATIQUE (insert/remove, jamais
          rebuild — contrairement aux segments), état initial ~25KB
          une fois puis quasi rien (profil naît/meurt). Artefact
          assumé : bouchée jugée à la tête SERVEUR (~RTT de retard
          sous lag, masqué plus tard par l'animation d'aspiration).
    [x] d. collisions serveur (grid rebuild/frame + findDeaths du proto) :
          tête vs corps adverse, bordure mortelle, mort -> cadavre semé,
          respawn. Grâce de spawn 3s.
          FAIT 2026-07-24, validé (kill en ligne, tête-à-tête double
          mort, bordure, self-cross légal, grâce translucide). Ajouts
          notables : Food.value synchronisé (orbes cadavre dorées x5,
          one-shot — seuls les pellets ambiants respawnent) ; compteur
          d'ÉPOQUE gen (uint8, % 256, égalité seulement) sur Player —
          un respawn est une TÉLÉPORTATION, gen change => les clients
          jettent interpolation/prédiction/corps (sinon lerp à travers
          la carte). Artefact assumé : mort jugée aux positions
          serveur (~RTT de "je l'avais esquivé", comme le vrai slither).
    [x] e. boost-drain par quanta d'orbes (port de l'algo A0.3).
          FAIT 2026-07-24, validé : dette 0.25/frame, quanta 1.5 à la
          queue, plancher (sprint coupé sous 1.5) MIROIR côté
          prédiction (sinon rubber-band). + RECYCLE_RATIO 30% appliqué
          au drain ET rétrofitté à la mort (le port initial faisait
          100% cadavre). ==> A1.7bis COMPLET : le jeu du proto tourne
          en multijoueur autoritatif avec le triptyque netcode.
    DÉCISION (user, 2026-07-24) : spawn de base = SCORE 0 (amende le
    FREE_PLAY_SCORE=100 du proto). Le score est un capital GAGNÉ en
    jouant : taille de base, sprint impossible avant d'avoir mangé
    (gating >= 1.5). Seule une mise supérieure (0.25/0.50/1 SOL)
    achètera un score de spawn — au port économie/menu.
    DÉCISION (user, 2026-07-24) : RECYCLE_RATIO=0.3 — toute valeur
    RELÂCHÉE (cadavre ET drain de sprint) route 30% de ses orbes
    (entières, conservation exacte) vers des positions map-wide
    aléatoires ; 70% restent sur place (cadavre / traînée de queue).
    Généralise le 70/30 d'A0.7 au sprint : l'offre ambiante se
    nourrit de TOUTE dépense, pas seulement des morts, et la richesse
    ne se concentre pas là où elle a été perdue.
    NB : rendu Pixi du proto porté plus tard (fin ARENA-1) ; on valide
    d'abord la MÉCANIQUE en cercles canvas.
[x] A1.8  Area of Interest : n'envoyer à chaque joueur que les entités proches.
          (Ligne accidentellement écrasée par l'insertion d'A1.7bis,
          restaurée.) FAIT (2026-07-24, validé + mesuré) : @view() sur
          la map food + StateView par client + diff de bulle
          (AOI_RADIUS 1200) toutes les 10 ticks via la grille spatiale.
          Joueurs NON filtrés (choix : 16 têtes = miettes, minimap) ->
          radar-à-joueurs encore scriptable, À FERMER avant l'argent
          réel (étendre l'AoI aux joueurs). MESURE : régime 2274 B/s
          (churn de bulle ~1.5KB/s en mouvement) vs 750 avant — le
          gain est ailleurs : état initial ~6x plus léger, coût
          indépendant de la taille du monde, et surtout ANTI-MAPHACK
          (la donnée lointaine ne quitte JAMAIS le serveur — seule
          protection incassable ; cacher côté client = obfuscation).
          Bulle rendue visible au client (cercle pointillé debug).
[x] A1.9  Rage-quit / déconnexion : serpent immobile et tuable + fenêtre de reconnexion.
          FAIT (2026-07-24, validé, règle AMENDÉE en cours de route) :
          déconnexion -> serpent gelé GRIS TRANSLUCIDE, INOFFENSIF
          (hors du monde de collision : ne tue plus, ne meurt plus au
          contact), puis transformé en orbes-cadavre SUR PLACE après
          DISCONNECT_TTL_TICKS (5s). Retour avant l'échéance (blip
          réseau) = reprise exacte, corps intact. DÉCISIONS :
          (1) « immobile et TUABLE » ABANDONNÉ après playtest : le
          corps gelé restant létal était un mur défensif quasi
          intuable -> le rage-quit devenait une PROTECTION. Nouvelle
          règle : inoffensif + TTL courte, la déconnexion coûte
          TOUJOURS tout le score (conservation : orbes au sol).
          (2) frozenBody = l'UNIQUE exception au « never sync bodies »
          (ArraySchema x,y rempli one-shot au freeze, vidé au retour,
          0 octet en régime normal) : un corps gelé ne peut pas être
          régénéré en observant le mouvement de la tête -> sans ça,
          hitbox invisible pour les late joiners (bug trouvé en test).
          (3) Le TICK est l'unique autorité sur l'expiration
          (allowReconnection "manual", rejetée par la simulation) —
          pas de second timer réseau qui pourrait diverger du jeu.
          (4) Bug client corrigé : figer aussi le follow-the-leader
          local, sinon le corps client s'effondre sur la tête en ~2s.
          ⚠ NOTÉ pour A4.6 : exploit théorique « esquive par
          débranchement » (couper la socket = intangible 5s puis
          reconnexion) — friction au retour à prévoir avant l'argent
          réel (ex : re-tangible après 1s de canalisation).
[x] A1.10 Test multijoueur réel avec latence simulée ; stabilité d'une room sous charge.
          FAIT (2026-07-24, validé + mesuré) : workspace loadtest/
          (bots.ts) — clients HEADLESS parlant le protocole exact du
          navigateur (join + intents 20Hz, rien d'autre) = preuve que
          la frontière autoritative est étanche, et SQUELETTE du bot
          red-team A4.6. Instrumentation [perf] permanente côté
          serveur (durée de tick = LA métrique de santé d'une room).
          MESURES (12 bots + 1 humain, room 13/16, bots longs) :
          tick avg ~3ms / max ~22ms vs budget 33.3ms -> ~9% utilisé,
          marge x10 ; client 6424 B/s (têtes globales + churn d'orbes)
          vs ~30KB/s naïf. Ressenti fluide avec latence simulée.
          LEÇONS D'ALGO BOT (3 itérations de playtest) :
          (1) « viser l'orbe la plus proche » = orbite infinie — zone
          aveugle du RAYON DE BRAQUAGE (R = v/ω ≈ 50px ; un point
          dans un des 2 cercles de virage est inatteignable en
          braquant) -> filtre géométrique + engagement de cible ;
          le boost DOUBLE R. (2) Bots aveugles aux autres serpents =
          boule de la mort (convergence sur les cadavres,
          entre-tuerie, ré-appât) -> fuite des têtes proches
          (corps invisibles : jamais synchronisés — limite notée,
          perception réelle = A4.6). (3) OBSERVATION ÉCONOMIQUE
          (user) : +3 orbes/s d'inflation en régime permanent —
          ARENA-1 n'a PAS de FoodReserve (respawn ambiant = robinet
          gratuit assumé) ; la simulation démontre en direct la
          pathologie exacte que D71/FoodReserve doit empêcher.
          A1.9 re-testé à l'échelle (Ctrl+C flotte = vague de gelés
          puis de cadavres, RAS).
==> JALON ATTEINT (2026-07-24) : une room jouable, fluide, autoritative, à
    plusieurs. Toujours sans argent.
[x] A1.11 (post-jalon) Portage Pixi + caméra du proto sur le client réseau.
          FAIT (2026-07-24, validé) : render.ts = couche Pixi pure
          (le netcode calcule les positions, la vue les affiche —
          même split sim/rendu que le proto). Texture partagée,
          SnakeView, labels, minimap SELF-ONLY (pas de radar joueurs
          dans l'UI), input proto (centre écran -> souris, boost
          clic/espace). LEÇON MAJEURE : la vue monde-entier (x0.117)
          MASQUAIT les artefacts netcode ; à l'échelle 1 + caméra,
          200ms simulées = quasi injouable (écart temporel cumulé
          ~300ms entre SA tête prédite (futur) et les ennemis
          interpolés (passé) = 70-140px de désaccord de collision).
          CORRECTIFS validés : (1) DEAD RECKONING — les autres sont
          projetés à leur PRÉSENT ESTIMÉ (copie locale auto-avancée
          au modèle partagé + convergence 15%/frame vers la
          projection du dernier snapshot, cap 300ms) ; remplace
          l'interpolation A1.6 (historique git) ; faux pendant leurs
          virages (borné par leur rayon de braquage). (2) CAMÉRA
          AMORTIE 15%/frame : les corrections A1.7 bougent la tête
          de quelques px au lieu de secouer le monde. RESTE
          INCOMPRESSIBLE : ~RTT/2 d'avance de sa propre tête ->
          vraie équité de collision = LAG COMPENSATION serveur
          (rewind), décision de design épineuse (favoriser qui ?),
          reportée au durcissement pré-argent réel. Barre de test :
          cible réelle = serveurs régionaux 30-80ms ; 200ms = stress.


--------------------------------------------------------------
PHASE ARENA-2 — PRIMITIF D'ESCROW ON-CHAIN  (programme Anchor GÉNÉRIQUE)
  But : le programme Solana, AGNOSTIQUE du jeu (réutilisable par le portail).
  Réutilise à fond les patterns loterie (PDA, vault, CPI, has_one, anti-rejeu).
  RÈGLE : pas de logique tant que les structs ne compilent pas (cf loterie phase 2).
--------------------------------------------------------------
[x] A2.1  Nouveau programme programs/arena/ (séparé, aucun état partagé loterie).
          -> FAIT (2026-07-24, make build vert) : keypair générée par
             l'utilisateur AVANT le 1er build (Program ID définitif dès
             declare_id! : 8qGdXYu3prvvvhCEfMnatdv5BAQyVL5NWsh7v5esEF2d),
             Cargo.toml répliqué de lottery (anchor-lang 0.31.1,
             overflow-checks=true), Cargo.lock COPIÉ de lottery (hérite
             les pins D41 — jamais de cargo update), Anchor.toml à jour,
             lib.rs = declare_id + module vide + modèle de confiance
             documenté EN CLAIR dans le doc-comment (y compris ce qui
             n'est PAS protégé). /!\ target/ non committé : la keypair
             arena est locale (à sauvegarder au déploiement devnet).
[x] A2.2  Structs SANS logique : Round PDA ["round", round_id], Vault PDA
          ["vault", round_id], FoodReserve PDA (global persistant), ExtractReceipt
          PDA ["extract", round_id, nonce]. Enum d'état + erreurs custom + tailles (space).
          -> FAIT (2026-07-28, retapé main, make build vert) : Round LEN=127
             (4 compteurs = registre de l'invariant D55 paid <= deposited
             + injected), Vault = SystemAccount SANS struct (D20bis),
             FoodReserve LEN=57 (singleton, données+lamports MÊME compte,
             jamais sous le rent-exempt), ExtractReceipt LEN=73 (anti-rejeu
             par EXISTENCE du PDA, init échoue si (round_id, nonce) déjà
             réglé), ArenaError 9 variantes. Rappel : un LEN faux compile —
             recompter à chaque relecture.
[x] A2.3  initialize_round(round_id, end_ts, server_authority, ...) : ouvre le round,
          injecte optionnellement depuis FoodReserve (VRAI transfert de lamports).
          -> FAIT (2026-07-28, retapé main + fixes, make build vert) : 3
             instructions — initialize_reserve (singleton, 1er appelant =
             authority, À APPELER DANS LA FOULÉE DU DÉPLOIEMENT sinon squat
             = vol de réserve via round hostile) ; initialize_round
             (permissionless, authority/treasury en args Pubkey, checks
             InvalidDuration + InvalidSplit casté u64, vault SystemAccount
             dérivé sans init, bumps stockés) ; inject_reserve (SÉPARÉE de
             l'init comme décidé : débit lamports DIRECT — la réserve est
             program-owned, pas de CPI System —, plancher rent-exempt,
             has_one authority, RoundNotOpen, compteurs checked_add).
[x] A2.4  join(stake) : dépôt VARIABLE permissionless dans le Vault (buy-in),
          SPLIT (D71) : rake 5.5% -> maison + ~2% pellet fund -> FoodReserve
          + solde = valeur de spawn. Event Joined. (= buy_ticket généralisé.)
          -> FAIT (2026-07-29, retapé main + fixes, make build vert) : 3 CPI
             System depuis le wallet joueur (inverse d'inject_reserve : le
             wallet est system-owned -> CPI + signature obligatoires),
             split par soustraction (les poussières de division vont au
             spawn_value : les 3 parts somment EXACTEMENT à stake), skip
             des CPI à 0 lamport (stakes minuscules), has_one = treasury
             (anti-substitution), DOUBLE verrou temporel (state Open ET
             now < end_timestamp — l'état ne bascule qu'à l'appel
             d'end_round), event Joined (pont serveur A3.2 / indexer).
[x] A2.5  settle_extraction(round, player, amount, nonce) : authority SIGNE (modèle A),
          anti-rejeu via création du ExtractReceipt PDA, garde amount <= vault.lamports.
          -> FAIT (2026-07-29, retapé main + fixes, make build vert) : NOUVEAU
             CONCEPT invoke_signed — le vault est un PDA system-owned SANS clé
             privée : CpiContext::new_with_signer + seeds [VAULT_SEED,
             round_id, vault_bump] (le bump fait partie de la preuve), le
             runtime re-dérive l'adresse avec NOTRE program id et accorde le
             statut signer le temps du CPI. Anti-rejeu = init du reçu (le
             runtime refuse, zéro require!), has_one = authority, garde
             amount <= lamports - minimum_balance(0) (plancher rent du
             vault ; les miettes partent au sweep d'A2.6), compteur
             total_paid (invariant D55), PAS de check end_timestamp
             (une extraction légitime réglée juste après la deadline reste
             due — seule la bascule d'état par end_round ferme la porte),
             rent du reçu payée par l'authority, event Extracted.
[x] A2.6  end_round() : balaie le solde restant du Vault vers FoodReserve.
          -> FAIT (2026-07-30, retapé main, make build vert) : double garde
             (state Open sinon RoundNotOpen + now >= end_timestamp sinon
             RoundNotEnded — personne, pas même l'authority, ne clôture en
             avance), authority-only (un end_round permissionless pourrait
             griefer les settlements de dernière seconde ; prix accepté :
             serveur mort = round ouvert, close permissionless après délai
             de grâce -> APROD), sweep INTÉGRAL via invoke_signed (vider à
             0 exactement est légal, le vault est réapé — seul
             0 < solde < plancher rent est interdit), la bascule state=Ended
             ferme join/inject/settle d'un coup, total_swept_in += sweep
             (SOL orphelin D50), event RoundEnded.
[x] A2.7  Events : Joined, Extracted, RoundEnded.
          -> FAIT au fil de l'eau (chaque event émis inline dans son
             instruction : A2.4/A2.5/A2.6). Les payloads seront prouvés
             par les tests A2.8 (listeners).
[x] A2.8  Tests Anchor (succès + ÉCHECS) : rejeu refusé, insolvabilité impossible,
          mauvaise authority, round fermé, double-settle, buy-in variable cohérent.
          -> FAIT (2026-07-30, tests/arena.ts, 21 tests) : 8 succès (split
             exact au lamport près avec stake NON-divisible, stake minuscule
             à parts tronquées, bumps stockés vs canoniques, sweep intégral,
             payloads des 3 events) + 13 échecs, chacun assertant le CODE
             D'ERREUR EXACT (un test qui passe sur la mauvaise erreur = un
             verrou qu'on croit fermé). Leçons de test apprises : assertions
             en DELTAS (la FoodReserve est un singleton partagé), plancher
             de rent du vault au premier join (le runtime refuse un dépôt
             sous ~0.00089 SOL dans un compte vide), .accountsPartial()
             (Anchor 0.30+ auto-résout les PDAs depuis l'IDL), listeners
             d'events armés TOUS EN AMONT (la souscription log partagée
             d'Anchor se démonte à zéro listener et race avec le re-add),
             ts-mocha transpile SANS typecheck (erreurs de type invisibles
             au run — lottery.ts en a 14, backlog cosmétique).
[x] A2.9  Prouver PAR TEST l'invariant de solvabilité : sum(payouts) <= dépôts + injecté.
          -> FAIT (2026-07-30) : scénario complet 3 joueurs (stakes variés
             non-divisibles), injection, 2 settles, p3 jamais sorti = SOL
             orphelin balayé. 4 assertions en escalade : compteurs on-chain
             exacts, invariant D55, soldes RÉELS (pas ce que le programme
             prétend), et l'équation de conservation finale :
             deltaTreasury + deltaReserve + payouts === somme des stakes.
==> JALON ATTEINT (2026-07-30) : anchor test VERT sur arena — 40 passing
    (22 arena + 18 lottery). Le socle escrow est prouvé par test.


--------------------------------------------------------------
PHASE ARENA-3 — INTÉGRATION SERVEUR <-> CHAÎNE  (settlement réel)
--------------------------------------------------------------
[x] A3.1  Sign-In With Solana (SIWS) : authentifier la session sans tx ; lier wallet <-> partie.
          -> IMPLÉMENTÉ (2026-07-30), prouvé HEADLESS 7/7 ; VALIDÉ
             NAVIGATEUR (2026-08-02, Phantom signIn réel). Deux fixes
             que le headless ne POUVAIT pas attraper :
             (1) STATEMENT ASCII-ONLY — l'ABNF SIWS (héritée d'EIP-4361)
                 interdit le non-ASCII ; le tiret cadratin « — » faisait
                 rejeter TOUTE la requête par Phantom (« invalid
                 format ») avant même d'afficher le dialogue.
             (2) FORME DE RÉPONSE — le provider INJECTÉ renvoie une
                 forme PLATE ({ address, signedMessage, signature }),
                 pas la forme wallet-standard ({ account: { address } }).
                 wallet.ts tolère les deux (extractAddress + log de la
                 forme brute en cas d'échec) : le wallet-standard est
                 la cible ARENA-5 (multi-wallets), et la tolérance
                 client est GRATUITE — le serveur re-vérifie tout, le
                 client peut se tromper mais pas mentir.
             Archi : shared/siws.ts (message SIWS canonique : build
             pour les bots, parse pour le serveur ; la sécurité tient
             à 3 champs signés : domain/address/nonce) ; serveur
             auth.ts (nonces 128 bits single-use TTL 2min en Map
             module-level + verifyAuthToken : ed25519 tweetnacl,
             AUCUNE confiance au payload avant vérif) ; route GET
             /auth/challenge (express hook de defineServer, CORS
             manuel) ; static onAuth (0.17, vérifié en doc : rejet
             AVANT onJoin, retour truthy -> client.auth) ; client
             wallet.ts (Phantom signIn = standard SIWS, anti-phishing
             par domaine côté wallet ET côté serveur). Player.wallet
             NON synchronisé (privacy : pas de wallet-stalking).
             PROTOCOL_VERSION 1 -> 2. Bots loadtest : keypair
             éphémère + signature maison = preuve que la porte exige
             la possession de la clé, rien d'autre (et rappel A4.4 :
             SIWS identifie, il n'anti-Sybil PAS — les clés sont
             gratuites). NOUVEAU loadtest/src/auth-attack.ts (probe
             adversarial permanent, embryon A4.6) : join honnête
             accepté ; sans jeton / jeton poubelle / REJEU (nonce
             consommé) / mauvais domaine / signature d'une autre clé
             que celle prétendue -> tous rejetés, raisons loggées
             serveur only. Deps : tweetnacl + bs58 (server, loadtest),
             @types/express (dev).
[ ] A3.2  Flux JOIN : le joueur dépose (join tx) AVANT de spawn ; le serveur vérifie
          le dépôt on-chain (ou via l'indexer) avant d'autoriser l'entrée.
          -> IMPLÉMENTÉ (2026-08-02), à cocher après E2E navigateur.
          DÉPLOYÉ DEVNET : programme arena 8qGdXYu3prvvvhCEfMnatdv5BAQy
          VL5NWsh7v5esEF2d (~2 SOL de rent ; recette lottery : build-sbf
          --arch v0 ; ⚠ SAUVEGARDER target/deploy/arena-keypair.json =
          upgrade authority, non committée). initialize_reserve appelé
          DANS LA FOULÉE (anti-squat A2.3) + round dev 24h ouvert via
          scripts/init-arena-devnet.ts (idempotent, re-lançable pour
          rouvrir un round : imprime le ROUND_ID). Round actif :
          1785673242 (authority/treasury = wallet dev FMzJ..iLYw).
          ARCHI (PROTOCOL_VERSION 2 -> 3) :
          - shared/arena-chain.ts : instruction join encodée À LA MAIN
            (discriminator sha256 de l'IDL + u64 LE, ordre des comptes
            = contrat IDL), PDAs, décodage du compte Round par offsets
            (127 o), miroir du split (spawnValueFromStake), et LE TAUX
            SOL<->score TRANCHÉ MVP : SCORE_PER_SOL=1000 (0.1 SOL net
            ~92.5 score, dans la plage de spawn du proto) — curseur,
            à ne changer qu'ENTRE rounds. Choix : PAS d'Anchor client
            navigateur (polyfills Vite) — web3.js seul, sans Buffer
            dans shared (DataView/TextEncoder).
          - client : menu.ts (port DOM d'A0.7bis, tiers FREE/0.10/
            0.25/0.50/1), wallet.ts sendJoinDeposit (GET /round -> tx
            join -> Phantom signAndSendTransaction -> confirmation avec
            lastValidBlockHeight), main.ts séquence menu -> SIWS ->
            dépôt -> joinOrCreate({stake, txSig}).
          - serveur : chain.ts (loadRound au BOOT depuis ROUND_ID env —
            round absent/fini = crash au boot, pas au 1er join ; sans
            env = mode FREE-ONLY loggé), GET /round (roundId en STRING
            — u64 > 2^53), verifyDeposit = LA porte : tx trouvée +
            succès + âge < 10min + instruction join de NOTRE programme
            + round = LE NÔTRE (initialize_round est permissionless !)
            + payeur = wallet SIWS (les signatures sont PUBLIQUES —
            sans ce lien, spawn avec le txSig d'autrui) + signature
            single-use (réservée AVANT le RPC, relâchée sur échec).
            Montant lu des OCTETS de la tx, jamais des options client.
          LIMITES ASSUMÉES (notées pour A3.4/A4) : consumedSigs en
          mémoire (restart = fenêtre de rejeu bornée par l'âge 10min ;
          vrai fix = persistance/indexer A3.4) ; mort = respawn FREE
          (le "repayer pour respawn" complet = menu de mort client,
          après l'E2E) ; free-only pas encore séparé du serveur payant
          (A0.7bis prod).
          LANCEMENT DEV : ROUND_ID=1785673242 npm run dev:server
[ ] A3.3  Service de settlement en Rust : détient la clé authority (hot wallet au MVP),
          écoute les extractions du serveur de jeu, soumet settle_extraction.
[ ] A3.4  Indexer arena (réutilise le pattern loterie) : events -> Postgres
          (historique, soldes, résultats de rounds).
[ ] A3.5  Gestion FoodReserve inter-rounds : report du SOL orphelin (transferts réels).
[ ] A3.6  E2E TESTNET : join -> jouer -> extract -> payout confirmé + reflété en DB.
==> JALON : une partie complète à argent (testnet) de bout en bout.


--------------------------------------------------------------
PHASE ARENA-4 — ANTI-TRICHE & MATCHMAKING  (le vrai mur technique)
  Bots = mur n°1 d'un jeu de skill à argent. Travail CONTINU, jamais "fini".
--------------------------------------------------------------
[ ] A4.0  ÉQUITÉ DE COLLISION SOUS LATENCE (constat user 2026-07-24, post-Pixi :
          hitbox non fiable à ~200ms RTT, correcte en dessous ~100ms).
          Deux solutions CONNUES, à combiner : (1) LAG COMPENSATION
          serveur (rewind ~500ms d'historique de positions, rejouer la
          collision au moment vécu par le joueur) — standard industrie
          MAIS décision de design épineuse en PvP à argent (les 2
          joueurs sont tireur ET cible ; piste : rewind pour sauver la
          victime, jamais pour valider un kill) ; (2) PING GATE : rooms
          à argent réel interdites au-dessus d'un seuil de RTT (mesure
          déjà dispo : ping/pong A1.7, affiché au HUD) + serveurs
          régionaux (cible réelle 30-80ms). Sans (2), (1) ne suffit pas.
[ ] A4.1  Revue : serveur autoritatif durci (aucune confiance au client) — audit.
[ ] A4.2  Matchmaking par TIER de mise / skill (ségrège whales / fish / bots ; résout
          shark-vs-fish et le whale-stomping du buy-in variable).
[ ] A4.3  Heuristiques de détection de bots (déplacements suspects) + shadow pool
          (bot containment : suspects isolés entre eux sans le savoir).
[ ] A4.4  Rate-limit d'entrée + anti-Sybil best-effort.
[ ] A4.5  Monitoring / alertes des dérives économiques (extraction anormale, feeding).
[ ] A4.6  RED-TEAM ÉCONOMIQUE (idée user, 2026-07-24) : construire NOUS-MÊMES
          le bot optimal (sur la base des clients headless d'A1.10) et
          mesurer, économie en place : (1) est-il tuable par un humain
          (hypothèse : oui — encerclement/blocage = géométrie, pas
          réflexes ; extraction = fenêtre de kill BY DESIGN) ;
          (2) surtout : son EV/heure net (rake + morts) en mode
          "rentier profil bas" (farm ambiant sans combat, extractions
          petites et fréquentes). EV > 0 = alarme rouge -> re-tuning
          (valeur ambiante D71, rake, tiers A4.2). Le vrai danger
          n'est pas le bot immortel, c'est le bot RENTABLE sans risque.
          (3) AJOUT (user, 2026-07-24, pendant A1.10) : au-delà du bot
          tricheur, une SIMULATION DE POPULATION RÉALISTE — bots aux
          profils de vrais joueurs (chasseur agressif, farmeur
          prudent, fuyard, kamikaze) qui mangent, attaquent, fuient —
          pour vérifier que l'économie interne (FoodReserve, 70/30,
          rake) reste viable sous une population vivante, pas
          seulement contre un attaquant isolé. Base : loadtest/bots.ts
          (A1.10), qui gère déjà protocole + ciblage d'orbes avec
          rayon de braquage ; il manque la perception des autres
          serpents (map players, déjà synchronisée globalement).
          (4) AJOUT (user, 2026-07-24) : SCÉNARIO BASSE FRÉQUENTATION
          (les débuts du jeu) — bots qui misent, accumulent, EXTRAIENT
          volontairement au-delà d'un seuil et NE respawnent PAS ;
          mesurer à N=2/3/5 : vitesse de drainage de l'arène, EV des
          premiers arrivés. ANALYSE PRÉALABLE (2026-07-24) :
          l'insolvabilité est IMPOSSIBLE par conservation (somme
          extractions <= somme mises - rake, à toute population) ; le
          vrai risque est l'ATTRACTIVITÉ (serveur vide = EV négative
          garantie sans upside -> spirale de désertion, cold-start
          type poker room). Leviers notés : concentrer la liquidité
          (1 room/tier), WORLD_RADIUS+FOOD_COUNT fonction de la
          population, injections marketing bornées au bootstrap
          (D71, botable -> après protections) ; REJETÉ d'office :
          bots maison financés house (= modèle LP par la porte de
          derrière + malhonnêteté vis-à-vis des joueurs).
          Prérequis : extraction + économie portées dans arena.
  NB : collusion-nourrissage DÉSAMORCÉE par le buy-in variable (D67) ; reste best-effort.


--------------------------------------------------------------
PHASE ARENA-5 — MONÉTISATION  (APRÈS un jeu FUN et peuplé)
  Un skin ne vaut rien sans joueurs qui aiment le jeu. On empile APRÈS.
--------------------------------------------------------------
[ ] A5.1  Rake 5.5% intégré au join (0.945 pot / 0.055 maison) + reporting du revenu.
[ ] A5.2  Skins NFT cosmétiques (Metaplex Core) : entitlement on-chain, rendu client,
          COSMÉTIQUE-ONLY (zéro pay-to-win).
[ ] A5.3  Marketplace primaire + secondaire (Tensor / Magic Eden), royalties.
[ ] A5.4  Loterie = gacha à skins (réutilise le programme lottery ; c'est ICI que
          la PARTIE 2 ci-dessous reprend du service).
          -> DÉBLOQUE PROD.1 : VRF Switchboard OBLIGATOIRE (skins à valeur réelle mainnet).
[ ] A5.5  Portail multi-jeux (jeux.fr-like) : brancher un 2e jeu (agar.io) pour
          valider la généricité du primitif escrow. Vision LT, pas MVP.
[ ] A5.6  IDÉE À TRANCHER (2026-07-23, pas un engagement) : "boost fee" —
          une part de la valeur drainée par le sprint est conservée en fees
          au lieu d'être resemée en orbes. Techniquement trivial (le serveur
          est le comptable ; matérialisation au end_round via split treasury,
          cf A2.6). /!\ DOIT être AFFICHÉE et documentée, jamais invisible :
          une fee cachée est mesurable par les joueurs (taux de drop vs
          drain) et sa découverte détruit la confiance — contraire au
          pilier "conservation visible" (A0.7). Évaluer le rake total
          effectif (entrée 5.5% + boost) avant d'empiler.


==============================================================
  /!\ ARENA — BLOQUANT AVANT MAINNET (vraie valeur)  /!\
  Rien de ceci n'est requis pour le MVP TESTNET, mais TOUT est
  OBLIGATOIRE avant de toucher de la vraie valeur.
==============================================================
[ ] APROD.1  ANTI-BOT niveau production : le mur n°1. Course à l'armement continue.
[ ] APROD.2  CUSTODY : clé authority en KMS/HSM + MULTISIG ; passer au Modèle B
             (attestation ed25519 vérifiée on-chain) + settlement optimiste / fraud-proofs.
[ ] APROD.3  VRF Switchboard pour la loterie-gacha (aléatoire non manipulable) (= PROD.1).
[ ] APROD.4  Audit sécurité sérieux du programme arena (escrow = fonds réels).
[ ] APROD.5  RÉSILIENCE : si le serveur crash en pleine partie, les fonds ne doivent
             pas rester bloqués -> mécanisme de timeout / remboursement ON-CHAIN.
[ ] APROD.6  JURIDIQUE : PARKÉ par décision utilisateur (domicilié Dubai). Noté, non traité
             (cf D68). Ne pas re-soulever.
[ ] APROD.7  INFRA SCALE : rooms / shards, orchestration (Agones/K8s), multi-région (latence).
==============================================================


##############################################################
  PARTIE 2 — LOTERIE (side-feature / futur gacha à skins)
  EN PAUSE depuis le pivot. Reprise prévue vers ARENA-5 (A5.4).
  La numérotation historique (9.x / 10.x / 11.x / PROD.x) est
  CONSERVÉE : DECISIONS.md et CLAUDE.md y font référence.
##############################################################

--------------------------------------------------------------
ÉTAT DES LIEUX — CE QUI EST FAIT ET VERT (phases A, 0..9, 10.A-C)
  (résumé opérationnel ; détail complet dans le git log de TODO.txt)
--------------------------------------------------------------
[x] Phases A/0/1 — env & workspace : Anchor 0.31.1 (D41), programme EXCLU du
    workspace racine (exclude=["programs"]) -> TOUJOURS tester via `make test`
    (exporte CARGO_TARGET_DIR vers target/ racine, sinon .so périmé déployé).
[x] Phases 2..7 — programme lottery COMPLET : initialize_lottery, buy_ticket,
    draw_winner, payout (modèle push, D26). 18 tests verts (succès + échecs).
    Revue sécurité passée (contraintes de comptes, checked_*, CEI, autorités).
    DÉPLOYÉ DEVNET (2026-06-20) : Program ID DD5CPAQWUtKSBajtNT9w4QbJysQnuWeDZ6yCdXKAYwro
      Recette : cargo build-sbf --manifest-path programs/lottery/Cargo.toml --arch v0
                puis solana program deploy --url devnet (CARGO_TARGET_DIR=racine).
      Smoke test OK (scripts/smoke-devnet.ts). Seed E2E OK (scripts/seed-devnet*.ts).
    -> Optionnel restant : publier l'IDL on-chain (make idl-devnet).
    /!\ L'aléa du draw = pseudo-random slot+timestamp : MANIPULABLE par un
        validateur. Acceptable devnet UNIQUEMENT (cf PROD.1 / APROD.3).
[x] Phases 8/8bis — DB Postgres : schéma aligné sur les events on-chain (ancres
    d'idempotence round_id / signature / (lottery_id, ticket_index)), repos Rust
    testés (#[sqlx::test]). Docker compose postgres:16 sur port hôte 5433.
    Rôle = cache RECONSTRUCTIBLE, zéro logique critique (D9).
[x] Phase 9 — INDEXER Rust complet : décodage events Anchor (discriminators IDL,
    garde-fou owner CPI-safe), dispatch idempotent (ON CONFLICT DO NOTHING,
    compteurs RECALCULÉS), daemon polling + curseur (table indexer_state),
    backfill paginé ancien->récent. Rejouabilité prouvée par test e2e synthétique.
    LIMITES CONNUES (assumées) :
     - winner_address rempli SEULEMENT au PrizeClaimed ; avant le claim,
       résoudre winner_index -> buyer via JOIN tickets si besoin d'affichage.
     - emit! passe par les logs -> RPC publics peuvent TRONQUER sous charge
       (event manqué). Parade = emit_cpi!, en backlog. Devnet = best effort.
     - commitment 'confirmed' (pas 'finalized') : à revoir avant prod.
[x] Phase 10.A-C — front app/ (Next.js 16, npm, App Router, Tailwind v4) :
    design system candy recolorié BLEU, homepage + /vaults (thème sombre)
    TERMINÉES visuellement mais encore MOCKÉES ; branding "Nimbo Play" fait.
    Plomberie web3 OK : providers wallet (Phantom/devnet), bouton connect,
    client Anchor (lib/anchor.ts), PDA côté front (lib/pda.ts), lecture du
    round via react-query (hooks/use-lottery.ts) + countdown (use-countdown.ts).
    Rappels techniques :
     - RE-COPIER src/lib/idl/ après chaque anchor build.
     - Round courant via env NEXT_PUBLIC_ROUND_ID (.env.local, actuel 1782381011).
     - Bloc debug TEMP <LotteryDebug /> sur /vaults à retirer au branchement (10.17).
     - Ne lancer QU'UN SEUL `npm run dev` (instances empilées = OOM).
     - UI "2 vaults live" = fiction : le programme n'a qu'UN round à la fois.

  NOTE POST-PIVOT (à trancher au moment de la reprise) : le front app/ est déjà
  brandé "Nimbo Play" -> vocation probable = devenir le PORTAIL du jeu ; les
  pages loterie/vaults seraient recyclées en UI du gacha à skins (A5.4).

--------------------------------------------------------------
RESTE À FAIRE LOTERIE — GELÉ jusqu'à la reprise (~A5.4)
--------------------------------------------------------------
  -- Écritures front (10.D) --
[ ] 10.11 buy_ticket : methods.buyTicket()...rpc(), signature wallet, refetch après confirmation.
[ ] 10.12 payout (claim) : pour le gagnant ; résoudre winner_index -> ticket -> buyer ;
          garde-fous UI (visible seulement si gagnant ET state=Closed ET !claimed).
[ ] 10.13 (admin, optionnel) initialize_lottery + draw_winner par l'authority = outil
          interne, PAS exposé au public.
  -- Contenu & branchement (10.E) --
[ ] 10.16 CORRIGER LE CONTENU vers la vérité (badge devnet, retirer "mainnet"/"VRF",
          UN seul round). Obligatoire cf CLAUDE.md (ne pas vendre l'aléa simple comme sûr).
[ ] 10.17 Brancher les sections sur les vraies données (remplacer les mocks par 10.C + 10.D).
  -- États UI & robustesse (10.F) --
[ ] 10.18 États UI : wallet déconnecté, tx pending (spinner), toasts succès/erreur (sonner),
          round closed/drawing, "tu as gagné -> claim", aucun round actif (empty state).
[ ] 10.19 Config RPC : gestion erreurs/retries basique, commitment 'confirmed', garde rate-limit.
  -- API REST & historique (9.7 + 10.G) --
[ ] 9.7   API REST en LECTURE seule pour le front (historique/stats), réécrite
          proprement par-dessus models/repositories (l'ancien scaffold est supprimé).
[ ] 10.20 (après 9.7) Lire l'historique depuis l'API REST Rust (PAS la chaîne directement).
[ ] 10.21 (après 9.7) Page historique des rounds + stats depuis la DB.
  -- Intégration finale (phase 11) --
[ ] 11.1  Scénario complet : init -> achats -> draw -> claim, via l'UI sur devnet.
[ ] 11.2  Vérifier que l'indexer/DB reflète bien tout le cycle.
[ ] 11.3  Cron qui appelle draw_winner à l'échéance.
[ ] 11.4  README : architecture, setup, lancement, captures.
[ ] 11.5  Nettoyage du code + commentaires + cohérence de nommage.
  -- Outillage --
[ ] 8bis.5 Dockeriser l'API Rust + l'indexer ; prod -> Postgres managé (Supabase/Neon/RDS).

--------------------------------------------------------------
/!\ LOTERIE — BLOQUANT AVANT TOUT USAGE RÉEL (gacha à valeur) /!\
--------------------------------------------------------------
[ ] PROD.1  ALÉATOIRE — remplacer IMPÉRATIVEMENT l'aléa on-chain "simple" par
            Switchboard VRF (l'aléa actuel est MANIPULABLE par un validateur ;
            l'aléa est ISOLÉ dans pseudo_random_index pour un swap quasi sans
            réécriture, cf D29/D30/D31). (= APROD.3, déclenché par A5.4.)
[ ] PROD.2  Audit interne sérieux du programme (contraintes de comptes,
            autorités, arithmétique).
[ ] PROD.3  Revoir le modèle d'autorité de draw_winner (cron centralisé = SPOF ;
            envisager permissionless + incitation, ou multisig).
[ ] PROD.4  Tests de charge / cas limites (beaucoup de tickets, collisions
            d'index concurrentes, rounds simultanés).
[ ] PROD.5  Gestion des fonds : rent-exemption du vault en toutes circonstances,
            close des comptes et récupération des lamports résiduels.
[ ] PROD.6  Mécanisme de remboursement / annulation si un round ne peut pas se
            conclure (0 ticket, bug, draw jamais appelé).

--------------------------------------------------------------
LOTERIE — BACKLOG PHASE 2 (ne pas commencer sans accord)
--------------------------------------------------------------
[ ] Système de tiers de prix (plusieurs gagnants, plusieurs paliers)
[ ] Tickets en SPL token
[ ] Page publique "provably fair" / vérification
[ ] Dashboard de stats
[ ] emit_cpi! à la place de emit! (events non tronquables par les RPC)
[ ] Déploiement mainnet (uniquement après audit interne sérieux)
==============================================================
