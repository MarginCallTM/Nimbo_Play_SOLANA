# Rust build artifacts
/target
**/target

# Environment files
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Anchor / JS artifacts
.anchor
test-ledger
node_modules
.yarn
# Working docs (project-internal: Claude instructions, decisions log,
# roadmap) — kept local, not for repo visitors
CLAUDE.md
TODO.txt
DECISIONS.md

# Runtime data: settlement outbox (claims/acks ledger, not code)
arena/server/settlement-outbox.jsonl

# Runtime data: consumed deposit-signature ledger (anti-replay, not code)
arena/server/consumed-deposits.jsonl

# A4.6 red-team fleet: devnet bot wallets (PRIVATE KEYS — devnet-only,
# but they never belong in a repo)
arena/loadtest/wallets/

# Deploy secrets: the mounted authority keypair (signs real payouts)
secrets/
